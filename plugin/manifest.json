{
  "schemaVersion": 1,
  "id": "user.ai-models",
  "name": "AI Models",
  "version": "1.0.0",
  "author": "user",
  "license": "MIT",
  "description": "Play/stop your local AI models (Ollama + raw GGUF via llama-server) from the Omarchy bar.",
  "kinds": ["bar-widget"],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "AI Models",
    "description": "One bar icon, one panel: every local model (Ollama + llama.cpp GGUF) with a play/stop button and live status.",
    "category": "AI",
    "aliases": ["ai-models", "models", "llm"],
    "allowMultiple": false,
    "defaults": {
      "refreshIntervalSec": 10
    },
    "schema": [
      { "key": "refreshIntervalSec", "type": "integer", "label": "Refresh interval (seconds)", "min": 3, "max": 120, "step": 1, "defaultValue": 10 }
    ]
  }
}
